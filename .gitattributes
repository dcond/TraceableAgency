# Preserve the reviewed paper snapshot byte-for-byte, including its source
# whitespace, while keeping Git's whitespace checks active for authored files.
Paper/trace_tempered_choice_v3.tex -whitespace
